# 测试案例编辑器 · 完整操作文档

> 适用插件：**TestCase Viewer**  
> 更新日期：2026-06-04

---

## 目录

1. [快速入门](#一快速入门)
2. [页面布局总览](#二页面布局总览)
3. [元信息栏](#三元信息栏)
4. [工具栏](#四工具栏)
5. [表格操作](#五表格操作)
6. [选区功能（Excel 风格）](#六选区功能excel-风格)
7. [右键菜单](#七右键菜单)
8. [列筛选功能（漏斗）](#八列筛选功能漏斗)
9. [查找与替换](#九查找与替换)
10. [推送功能](#十推送功能)
11. [资源管理器右键推送](#十一资源管理器右键推送)
12. [本地修改标记](#十二本地修改标记)
13. [全局快捷键](#十三全局快捷键)
14. [数据高亮标记说明](#十四数据高亮标记说明)
15. [弹窗功能](#十五弹窗功能)
16. [冻结列（testcase_id）保护规则](#十六冻结列testcase_id保护规则)
17. [过滤模式特殊行为](#十七过滤模式特殊行为)
18. [虚拟滚动（大数据集优化）](#十八虚拟滚动大数据集优化)
19. [状态持久化](#十九状态持久化)
20. [常用操作流程](#二十常用操作流程)

---

## 一、快速入门

### 打开编辑器

在 VS Code 中**单击**工作区中的 CSV、YAML 或 JSON 测试案例文件，即可自动以测试案例编辑器打开。

### 基本操作速查

| 想要做什么 | 操作方式 |
|------------|----------|
| 编辑单元格 | 双击目标单元格 |
| 添加测试案例 | 右键单元格 → 在上方/下方插入行 |
| 修改后推送 | 选中行 → 点击**推送测试案例**按钮 |
| 查找内容 | `Ctrl/Cmd + F` |
| 保存文件 | `Ctrl/Cmd + S`（编辑后自动已保存，无需手动触发） |
| 撤销操作 | `Ctrl/Cmd + Z` |
| 切换到文本编辑 | 工具栏点击 📄 图标 |

---

## 二、页面布局总览

```
┌─────────────────────────────────────────────────┐
│  元信息栏：任务号 | 任务名称 | 子任务 | 绑定状态  │
├─────────────────────────────────────────────────┤
│  工具栏：搜索 | 筛选按钮 | 推送 | 查找 | 选区信息  │
├──────┬──────────────────────────────────────────┤
│ 行号 │  表头① | 表头② | 表头③ | ... | 表头(N)  │
│  #1  │  单元格 │ 单元格 │ 单元格 │ ... │ 单元格  │
│  #2  │  单元格 │ 单元格 │ 单元格 │ ... │ 单元格  │
│  #3  │  单元格 │ 单元格 │ 单元格 │ ... │ 单元格  │
│ ...  │  ...   │  ...   │  ...  │ ... │  ...    │
├──────┴──────────────────────────────────────────┤
│  右键菜单 | 步骤明细弹窗 | 推送结果弹窗 ...      │
└─────────────────────────────────────────────────┘
```

---

## 三、元信息栏

位于页面最顶部，显示当前文件的绑定信息：

| 显示项 | 说明 |
|--------|------|
| 绑定状态标签 | 绿色=已绑定，灰色=未绑定 |
| 测试任务号 | 当前文件绑定的测试任务编号 |
| 测试任务名称 | 绑定的测试任务名称 |
| 子测试任务名称 | 绑定的子测试任务名称 |

---

## 四、工具栏

### 4.1 搜索与重置

**搜索框**：输入关键词即可实时过滤表格行（150ms 防抖），在所有列中匹配。`Enter` 立即搜索，`Esc` 或点击 `X` 清空。

**重置按钮 ⟳**：一键清空搜索 + 列筛选 + 数据筛选 + 查找面板，并请求从磁盘重新读取最新数据。

### 4.2 数据筛选按钮

四个**互斥**的筛选按钮，同一时间只能激活一个：

| 按钮 | 功能 | 可用条件 |
|------|------|----------|
| ⚠️ 仅看推送失败 | 只显示推送失败的行（红色底色） | 存在推送失败行 |
| ✏️ 仅看已修改 | 只显示本地有修改但未推送的行 | 存在本地修改 |
| 🟢 仅看新增 | 只显示新增但未推送的行（绿色底色） | 存在新增行 |
| 🗑️ 仅看已删除 | 以幽灵行样式展示已删除的行 | 存在删除操作 |

> 点击已激活的按钮可取消该筛选。

### 4.3 操作按钮

| 按钮 | 功能 | 说明 |
|------|------|------|
| **推送测试案例**（蓝色） | 将选中行推送到后端系统 | 无选中行时禁用；推送中显示"推送中…"；30秒超时自动解锁 |
| 🔍 查找 | 打开/关闭查找替换面板 | 等同 `Ctrl/Cmd + F` |
| 📄 文本编辑器 | 用 VS Code 默认文本编辑器打开当前文件 | 适合批量编辑 |
| 选区信息（文字） | 实时显示 `已选 N 行，共 M 行` 或 `矩形选区 R×C` | — |

---

## 五、表格操作

### 5.1 单元格编辑

| 操作 | 方式 | 说明 |
|------|------|------|
| **进入编辑** | 双击单元格 | 变为 textarea 输入框 |
| **提交编辑** | `Enter` | 保存修改，退出编辑模式 |
| **取消编辑** | `Esc` | 放弃修改，退出编辑模式 |

特殊编辑模式：
- **多行编辑**：行高被拉高后，`Enter` 换行，`Ctrl/Cmd + Enter` 提交
- **明细列**：双击打开步骤明细弹窗（不进入 textarea）
- **数组列**：双击打开数组编辑弹窗（不进入 textarea）
- **批量填充**：选区为矩形时，编辑一个格的值会写入整片选区（跳过冻结列）

### 5.2 行操作

| 操作 | 方式 | 说明 |
|------|------|------|
| 在上方/下方插入行 | 右键 → 在上方/下方插入行 | 自动生成 UUID 的 testcase_id |
| 在下方复制行 | 右键 → 在下方复制此行 | 深拷贝（testcase_id 重新生成，testCaseNo 清空） |
| 删除行 | 右键 → 删除该行 | 保存 ghost 信息，可在"仅看已删除"中查看 |
| 批量删除 | 选中多行 → 右键 → 删除选中行 (N) | 按降序删除 |

### 5.3 列操作

| 操作 | 方式 | 说明 |
|------|------|------|
| 插入列（左/右） | 右键表头 → 在左/右侧插入列 | 弹出输入框输入列名 |
| 删除列 | 右键表头 → 删除该列 | testcase_id 列禁止删除 |
| 重命名列 | 右键表头 → 重命名列 | testcase_id 列禁止重命名 |
| 清空选中列 | 选中多列 → 右键 → 清空选中列 (N) | 跳过冻结列和隐藏行 |
| 批量填充选中列 | 选中多列 → 右键 → 批量填充选中列 (N)… | 跳过冻结列；数组列以 `; ` 拆分 |

### 5.4 列宽与行高调整

| 操作 | 方式 |
|------|------|
| **拖动调整列宽** | 拖动列头右侧分割线（最小 40px） |
| **双击自适应列宽** | 双击列头右侧分割线（自动测量内容宽度，上限 600px） |
| **拖动调整行高** | 拖动行号格下方分割线（最小 24px） |
| **双击自适应行高** | 双击行号格下方分割线（自动测量内容高度） |
| **双击恢复默认行高** | 双击行号格 |

---

## 六、选区功能（Excel 风格）

### 6.1 行选

| 操作 | 行为 |
|------|------|
| 单击行号格 | 选中该行 |
| `Shift` + 点击行号格 | 区间扩展选行 |
| `Ctrl/Cmd` + 点击行号格 | 离散追加/取消选行 |
| 按住拖动行号格 | 横扫连续选行 |

### 6.2 列选

| 操作 | 行为 |
|------|------|
| 单击表头 | 选中该列 |
| `Shift` + 点击表头 | 区间扩展选列 |
| `Ctrl/Cmd` + 点击表头 | 离散追加/取消选列 |
| 按住拖动表头 | 横扫连续选列 |

### 6.3 单元格矩形选区

| 操作 | 行为 |
|------|------|
| 单击单元格 | 单选该格 |
| `Shift` + 点击单元格 | 以锚点扩展到目标（矩形选区） |
| 按住拖动单元格 | 实时跟随鼠标形成矩形（纵向主导自动锁列） |
| 点击左上角 `#` 角格 | 全选整表 |
| 再次点击 `#` 角格 | 取消全选 |

### 6.4 互斥规则

行选、列选、单元格选区**互相排斥**：激活一种，另外两种自动清除。

### 6.5 过滤模式下的选区

开启任何筛选/过滤时，选区只覆盖可见行，不会误选被隐藏的行。全选（`Ctrl/Cmd + A`）也只全选可见行。

---

## 七、右键菜单

### 7.1 表头右键菜单

| 菜单项 | 功能 | 禁用条件 |
|--------|------|----------|
| 在左侧插入列 | `insertCol(S._ctxCol)` | — |
| 在右侧插入列 | `insertCol(S._ctxCol + 1)` | — |
| ── | | |
| 删除该列 | 删除当前列 | testcase_id 列灰显 |
| 重命名列 | 修改列名 | testcase_id 列灰显 |
| ── | 以下仅在有列选区时出现 | |
| 清空选中列 (N) | 清空所有选中列 | 全为冻结列时灰显 |
| 批量填充选中列 (N)… | 批量填充选中列 | 全为冻结列时灰显 |

### 7.2 单元格右键菜单

| 菜单项 | 功能 | 禁用条件 |
|--------|------|----------|
| **推送测试案例 (N)** | 推送选中行 | 无选中行时灰显 |
| ── | | |
| 复制选区 (R×C) | 复制到剪贴板 | 行列下标无效时灰显 |
| 粘贴单元格 | 从剪贴板粘贴 | 剪贴板为空时灰显 |
| 清空选区 (R×C) | 清空选中区域 | 列下标无效时灰显 |
| 批量填充选区 (R×C)… | 填充整个矩形选区 | 选区 ≤ 1 格时不出；全为冻结列灰显 |
| ── | | |
| 在下方复制此行 | 深拷贝行到下方 | 行下标无效时灰显 |
| 在上方插入行 | 插入空白行 | 行下标无效时灰显 |
| 在下方插入行 | 插入空白行 | 行下标无效时灰显 |
| ── | | |
| 插入列（左侧） | 插入新列 | 列下标无效时灰显 |
| 插入列（右侧） | 插入新列 | 列下标无效时灰显 |
| 重命名列 | 修改列名 | testcase_id 列灰显 |
| 删除该列 | 删除当前列 | testcase_id 列灰显 |
| ── | | |
| 删除该行 | 删除当前行 | 行下标无效时灰显 |
| 删除选中行 (N) | 批量删除选中行 | 仅在有行选区时出现 |
| ── | 以下仅在有列选区时出现 | |
| 清空选中列 (N) | 清空选中列内容 | 全为冻结列时灰显 |
| 批量填充选中列 (N)… | 填充选中列 | 全为冻结列时灰显 |

---

## 八、列筛选功能（漏斗）

每个表头右侧有一个漏斗图标 🔽，点击打开该列的筛选弹窗：

| 元素 | 功能 |
|------|------|
| 搜索框 | 在弹窗内搜索值（自动选中命中项） |
| 清除搜索 | 清空搜索词，恢复全选 |
| 重置 ⟳ | 清空搜索 + 恢复全选 |
| 全选 | 全选/反选当前可见值列表 |
| 清除筛选 | 移除该列的筛选条件 |
| 取消 | 关闭弹窗不应用 |
| 确定 | 应用筛选（至少需选一项） |

**特性**：
- 跨列联动计数：某列弹窗中的计数是"其他列筛选 + 搜索"通过后的行数
- 自动失效保护：编辑后若筛选不再匹配任何行，自动清除并提示
- 空白值显示为 `(空白)`，内部 key 为 `__BLANK__`
- 有筛选条件的列，漏斗图标高亮为蓝色

---

## 九、查找与替换

通过 🔍 按钮或 `Ctrl/Cmd + F` 打开查找替换面板：

| 元素 | 功能 |
|------|------|
| 查找输入框 | 输入即搜索，自动同步工具栏搜索框 |
| Aa 大小写开关 | 切换区分大小写 |
| 上一个 ↑ / 下一个 ↓ | 导航匹配项 |
| 匹配信息 | 显示 `N / M` 格式 |
| 替换输入框 | 输入替换文本 |
| 替换 | 替换当前匹配项（仅该格第一处） |
| 全部替换 | 替换表格中所有匹配项 |

**限制**：testcase_id 列跳过替换；数组列（string[]/number[]）不支持；过滤模式只搜可见行。

**视觉反馈**：命中格黄色高亮（`highlight`），当前活动格深色（`highlight-active`），匹配文本 `<mark>` 标签。

---

## 十、推送功能

### 10.1 推送方式

| 方式 | 操作 |
|------|------|
| 工具栏推送 | 选中目标行 → 点击**推送测试案例**按钮 |
| 右键推送 | 选中目标行 → 右键 → 推送测试案例 (N) |
| 单行快速推送 | 右键某行 → 推送（无选区时自动以右键行作为目标） |

### 10.2 推送流程

1. 选中需要推送的行（可多选）
2. 点击推送按钮
3. 按钮显示"推送中…"并置灰（防重复点击，30 秒超时自动解锁）
4. 弹出推送结果弹窗

### 10.3 推送结果弹窗

| 状态 | 颜色 | 说明 |
|------|------|------|
| 全部成功 | 绿色 | 所有选中行推送成功 |
| 部分成功 | 橙色 | 部分行成功，部分失败（显示失败列表） |
| 全部失败 | 红色 | 所有行失败（显示全部失败列表） |

弹窗功能：失败行明细列表、悬停显示详情、点击行号跳转到主表、一键复制失败明细。

### 10.4 推送后行为

| 行为 | 说明 |
|------|------|
| 回写 testCaseNo | 服务器返回的 testCaseNo 自动回写（绿色高亮） |
| 修改高亮清除 | 推送成功的行，黄色修改高亮消失 |
| 新增高亮清除 | 推送成功的新增行，绿色背景消失 |
| 失败行标记 | 失败行以红色底色高亮，后续可基于"仅看失败"筛选 |
| 累积合并 | 历史失败行保持高亮，本批成功行从失败集合移除 |

---

## 十一、资源管理器右键推送

除了在编辑器内部推送外，还可以直接从 VS Code **资源管理器**中右键推送，无需打开编辑器。

### 11.1 使用方式

1. 在 VS Code **资源管理器**（左侧文件树）中，找到需要推送的测试案例文件
2. 右键点击文件 → 选择 **推送测试案例**
3. 系统自动打开编辑器（如未打开）并执行推送
4. 推送结果以弹窗形式展示

### 11.2 适用条件

右键菜单中的"推送测试案例"选项仅在文件路径满足以下条件时出现：

```
测试任务/<任意任务名>/测试案例/<任意目录>/xxx.csv
测试任务/<任意任务名>/测试案例/<任意目录>/xxx.yaml
测试任务/<任意任务名>/测试案例/<任意目录>/xxx.yml
测试任务/<任意任务名>/测试案例/<任意目录>/xxx.json
```

即文件必须位于 `测试任务/**/测试案例/` 目录结构下，且是 CSV、YAML、YML 或 JSON 格式。

### 11.3 推送流程

```
资源管理器右键文件
  → 检查文件是否在合规目录下
  → 检查任务是否已绑定（未绑定时弹窗提示）
  → （若编辑器未打开）自动用测试案例编辑器打开文件
  → 等待编辑器前端就绪（最长 8 秒）
  → 解析文件内容为二维数组
  → 调用后端 HTTP 接口推送
  → 推送成功项回写 testCaseNo 到文件
  → 更新快照基线
  → 弹出推送结果弹窗（全部成功/部分成功/全部失败）
```

### 11.4 与编辑器内推送的区别

| 维度 | 资源管理器右键推送 | 编辑器内推送 |
|------|-------------------|-------------|
| 入口 | 文件树右键菜单 | 表格内按钮 / 右键菜单 |
| 编辑器状态 | 自动打开（如未开） | 必须已在编辑器中 |
| 推送范围 | **整个文件**（全部行） | 手动选中的行 |
| 适用场景 | 全新文件首次推送、批量全量同步 | 增量修改、按需推送特定行 |

> ⚠️ **注意**：资源管理器右键推送是**全文件推送**，会将整个文件的所有行都提交到后端。如需按行推送，请在编辑器内选中目标行后推送。

---

## 十二、本地修改标记

| 标记 | 视觉效果 | 含义 | 何时消失 |
|------|----------|------|----------|
| 修改高亮 | 单元格黄色背景 | 该格被编辑但未推送 | 推送成功 |
| 新增高亮 | 整行绿色背景 | 本地新插入的行 | 推送成功 / 删除行 |
| 删除行 | 灰色条纹 ghost 样式 | 已删除但变更未推送 | 推送/撤销变更 |
| 推送失败 | 整行红色背景 | 上次推送失败 | 重新推送成功 |
| 推送回写 | 单元格绿色/橙色 | 推送后服务器回写的值 | 下次推送/刷新 |

---

## 十三、全局快捷键

| 快捷键 | 功能 | 限制 |
|--------|------|------|
| `Ctrl/Cmd + S` | 保存文件 | — |
| `Ctrl/Cmd + F` | 打开查找替换面板 | 弹窗打开时不拦截 |
| `Ctrl/Cmd + Z` | 撤销（最多 100 步） | 编辑态/弹窗中不拦截 |
| `Ctrl/Cmd + Shift + Z` / `Y` | 重做 | 编辑态/弹窗中不拦截 |
| `Ctrl/Cmd + C` | 复制选区（TSV 格式） | 编辑态/输入控件内不拦截 |
| `Ctrl/Cmd + V` | 粘贴 TSV（跳过 testcase_id） | 编辑态/输入控件内不拦截 |
| `Ctrl/Cmd + A` | 全选可见行 | 编辑态/输入控件内不拦截 |
| `Delete` / `Backspace` | 清空当前选区 | 编辑态/输入控件内不涉及 |
| `Escape` | 关闭弹出层（逐层关闭） | — |

---

## 十四、数据高亮标记说明

### 单元格级标记

| Class / 视觉 | 含义 |
|--------------|------|
| `xs-td-frozen` | 冻结列（testcase_id），不可编辑 |
| `xs-td-ghost` | 已删除行的幽灵单元格 |
| `xs-td-push-updated` | 推送后服务器回写的字段（绿色） |
| `xs-td-push-updated-row` | 推送后行内变化的字段（橙色） |
| `xs-td-push-added` | 新增行的单元格（绿色底色） |
| `modified` | 本地修改未推送的单元格（黄色） |
| `xs-cell-selected` | 矩形选区内的单元格（蓝色边框） |
| `xs-editing` | 正在编辑中的单元格 |
| `xs-detail-link` | 明细列（蓝色链接样式） |
| `xs-arr-cell` | 数组列单元格 |

### 行级标记

| Class | 含义 |
|-------|------|
| `xs-tr-push-failed` | 推送失败的行（红色底色） |
| `xs-ghost-row` | 已删除行的幽灵显示 |
| `selected` | 被行选中的行 |

### 查找高亮

| Class | 含义 |
|-------|------|
| `highlight` | 查找命中的单元格 |
| `highlight-active` | 查找当前活动命中的单元格（更深色调） |

---

## 十五、弹窗功能

### 15.1 步骤明细弹窗（双栏布局）

**打开**：双击明细列单元格（蓝色链接样式）。

```
┌──────────────┬─────────────────────┐
│  步骤列表    │   字段编辑区         │
│  · 步骤1     │   字段名: [值]      │
│  · 步骤2 ✓   │   操作: [值]        │
│  · 步骤3     │   参数: [值]        │
│  ＋ 添加步骤  │                     │
│  复制 步骤    │                     │
└──────────────┴─────────────────────┘
```

| 功能 | 操作 |
|------|------|
| 切换步骤 | 点击左栏步骤项 |
| 添加步骤 | 点击"+ 添加步骤" |
| 复制步骤 | 点击"复制"按钮 |
| 删除步骤 | 点击步骤右侧 `×`（嵌套对象类型不显示） |
| 标量字段编辑 | textarea 直接输入 |
| 数组字段编辑 | 每项独立 textarea，可添加/删除/排序 |
| 对象字段编辑 | JSON 编辑器模式 |
| 保存 | 点击"保存"按钮；取消则还原快照 |
| 关闭 | ✕ / 遮罩 / Esc |

### 15.2 数组列编辑弹窗

**打开**：双击数组列（string[]/number[]）单元格（chip 标签样式）。

| 功能 | 操作 |
|------|------|
| 编辑项 | 每项一个 textarea |
| 添加项 | 点击"+ 添加项" / 最后一项按 Enter |
| 删除项 | 点击项右侧 `×` |
| 排序 | 点击 ▲/▼ 上下移动 |
| 多行粘贴拆分 | 粘贴含换行/分号内容自动拆分 |
| 数字校验 | number[] 列实时检查数字合法性 |
| 保存 | 点击"保存" / `Ctrl/Cmd + Enter` |
| 取消 | 取消 / Esc / ✕ / 遮罩 |

### 15.3 通用弹窗

因 Webview 沙箱限制，使用自定义弹窗替代原生 `window.prompt()` / `window.confirm()`：

| 类型 | 场景 | 确认方式 |
|------|------|----------|
| 输入弹窗 | 插入列名、重命名列、批量填充 | Enter 确认 / Esc 取消 |
| 确认弹窗 | 删除列、删除步骤 | 点击确定 / 取消 |

---

## 十六、冻结列（testcase_id）保护规则

| 操作 | 保护行为 |
|------|----------|
| 双击编辑 | 提示"不允许编辑" |
| 粘贴 | 跳过该列，提示"testcase_id 列已跳过" |
| 清空（Delete/右键清空） | 提示"不允许清空" |
| 批量填充 | 跳过该列 |
| 删除列 | 提示"不允许删除" |
| 重命名列 | 提示"不允许重命名" |
| 查找替换 | 跳过该列 |
| 插入行 | 自动生成 UUID v4 作为 testcase_id |
| 复制行 | 自动重生成 UUID 的 testcase_id，清空 testCaseNo |

---

## 十七、过滤模式特殊行为

在开启搜索/列筛选/数据筛选时：

| 操作 | 行为 |
|------|------|
| 复制（Ctrl+C） | 只复制可见行（TSV 格式） |
| 粘贴（Ctrl+V） | 隐藏行不接收粘贴 |
| 清空/批量填充 | 只操作可见行 |
| 全选（Ctrl+A） | 只全选可见行 |
| 行选横扫 | 只选可见行 |
| 查找替换 | 只搜索可见行 |
| 四种筛选互斥 | 失败/修改/新增/已删除→只能激活一个 |

---

## 十八、虚拟滚动（大数据集优化）

当表格行数 ≥ **500 行**时自动启用：

| 参数 | 默认值 |
|------|--------|
| 启用阈值 | 500 行 |
| 估算行高 | 26px |
| 缓冲行数 | 上下各 10 行 |
| 滚动节流 | `requestAnimationFrame` |
| 行定位 | 二分查找 |

> 虚拟滚动模式下，所有交互（编辑、选择、右键等）均无感知差异。

---

## 十九、状态持久化

编辑器通过 `vscode.getState/setState` 持久化以下状态（按文件类型隔离）：

| 持久化项 | 写入时机 |
|----------|----------|
| 列宽 | 600ms 防抖写入 |
| 行高 | 600ms 防抖写入 |
| 列筛选条件 | 筛选变更时 |
| 搜索关键词 | 搜索变更时 |
| 滚动位置 | 滚动时防抖写入 |

切换 Tab 再切回时，这些状态自动恢复。

---

## 二十、常用操作流程

### 添加新测试案例并推送

1. 右键某行 → 在下方插入行
2. 双击填充新行各字段
3. 按 `Ctrl/Cmd + S` 保存
4. 选中新行 → 点击"推送测试案例"
5. 查看推送结果弹窗确认

### 批量修改并推送

1. 开启"仅看已修改"筛选确认修改无误
2. 开启"仅看失败"查看是否有历史失败行需同推
3. `Ctrl/Cmd + A` 全选可见行
4. 点击"推送测试案例"

### 复制行并修改后推送

1. 右键目标行 → 在下方复制此行
2. 修改需要变更的字段
3. 选中新行 → 推送

### 查找并批量替换

1. `Ctrl/Cmd + F` 打开查找替换面板
2. 输入查找文本 → 查看匹配项
3. 输入替换文本 → 点击"全部替换"
4. 推送修改过的行

### 列筛选精准定位

1. 点击目标列的漏斗图标 🔽
2. 在弹窗中勾选需要的值
3. 点击"确定"应用筛选
4. 对筛选结果进行操作

### 恢复误操作

- **撤销**：`Ctrl/Cmd + Z`（最多 100 步）
- **重做**：`Ctrl/Cmd + Shift + Z` 或 `Ctrl/Cmd + Y`
- **查看已删除行**：开启"仅看已删除"筛选
- **一键重置**：点击工具栏 ⟳ 重置按钮

### 资源管理器右键全文件推送

1. 在 VS Code 左侧资源管理器中，展开 `测试任务/xxx/测试案例/` 目录
2. 右键目标 CSV/YAML/JSON 文件 → 选择**推送测试案例**
3. 等待自动打开编辑器并完成推送
4. 查看推送结果弹窗（全量成功/部分失败明细）

> 💡 适合全新文件首次提交全量数据时使用

---

> **提示**：本文档基于 TestCase Viewer 插件实际功能编写，截图和视觉细节请以插件实际运行效果为准。
