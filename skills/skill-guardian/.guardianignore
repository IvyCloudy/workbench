# .guardianignore — 由 skill-guardian 读取，用于扩展 walk() 的排除列表
# 规则与 .gitignore 类似：一行一条，# 注释行忽略；支持精确路径 / 目录（以 / 结尾）/ 简易 glob
# 硬内置排除已包含 node_modules/.git/dist/__pycache__/.venv 等，这里无需重复

# --- 常见构建产物 ---
*.min.js
*.min.js.map

# --- IDE 与本地缓存 ---
.DS_Store
Thumbs.db

# --- CI / 日志 ---
*.log
tmp/
