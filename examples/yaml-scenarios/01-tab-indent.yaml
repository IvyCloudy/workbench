# ============================================================================
# 场景 01：R1 · Tab 缩进（error，stopOnHit）
# ----------------------------------------------------------------------------
# 期望：所有以 Tab 开头的行只命中 R1，不再叠加其它规则；level=error。
# 触发点：第 11、12、13、17、18 行（Tab 开头）。
# ============================================================================
metadata:
  name: tab-indent-showcase
  version: 1.0

	author: myron
	desc: hello
	enabled: "true"

steps:
  - operation: click
	  target: btn_login
	  timeout: 5
