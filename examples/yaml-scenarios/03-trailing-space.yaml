# ============================================================================
# 场景 03：R3 · 行末多余空格
# ----------------------------------------------------------------------------
# 期望：只命中 R3（level=info/warning），修复策略：删掉行尾空格。
# 注意：所有"看起来正常"的行末，其实都追加了 1~N 个空格。
# ============================================================================
metadata:
  name: trailing-space   
  version: 1.0     
  desc: hello world  
  author: myron 

steps:
  - operation: click  
    target: btn_ok    
  - operation: input   
    target: input_name
