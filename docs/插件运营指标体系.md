# 插件运营指标体系

> 本文档基于当前代码库中已落地的全部埋点（约 90 个事件），整理出一套可直接交付给数据/运营同学的运营指标。
>
> 全部事件上报至 `/api/v1/track`，每条事件自动注入以下公共属性：
>
> `extName / extVersion / vscodeVersion / platform / arch / nodeVersion / osRelease / language / machineId / sessionId / ts`

---

## 一、规模 & 健康度（北极星 + 基础盘）

| 指标 | 计算口径 | 数据来源 |
|---|---|---|
| **DAU / WAU / MAU** | `distinct(machineId)` 按时间窗去重 | `extension.activated` |
| **新增用户数** | 首次出现的 `machineId` | `extension.activated`（首次） |
| **留存（次日 / 7 日 / 30 日）** | 首次激活后 N 天再次激活的占比 | `extension.activated` |
| **会话数** | `count(distinct sessionId)` | `extension.activated` |
| **会话时长 P50/P95** | `extension.deactivated.sessionMs` | `extension.deactivated` |
| **激活耗时 P50/P95** | `activateMs` | `extension.activate.done` |
| **版本分布** | 按 `extVersion` 分组 | 公共属性 |
| **VSCode 版本 / 平台 / OS / 语言分布** | 按 `vscodeVersion / platform / osRelease / language` 分组 | 公共属性 |

> 这些指标用于回答"插件总盘子有多大、版本/平台分布、用户粘性"。

---

## 二、核心功能使用漏斗

### 2.1 推送漏斗（最重业务）

资源管理器右键推送和编辑器内推送是两条独立漏斗，可以分开看也可以合并看。

| 漏斗节点 | 资源管理器入口 | 编辑器入口 |
|---|---|---|
| ① 命令触发 | `command.executed[command=testcaseViewer.pushTestCaseFromExplorer]` | — |
| ② 启动推送 | `explorerPush.start` | `editorPush.start` |
| ③ 后端调用 | `api.pushTestCase.ok` / `api.pushTestCase.fail` / `api.pushTestCase.exception` | 同左 |
| ④ 完成推送 | `explorerPush.complete` | `editorPush.complete` |

**关键运营指标**：

| 指标 | 计算口径 | 字段 |
|---|---|---|
| 推送启动数 | `count(*.start)` | — |
| 推送完成数 | `count(*.complete)` | — |
| **推送成功率** | `sum(successRows) / sum(totalRows)` | `*.complete.successRows / totalRows` |
| **行级失败率** | `sum(failedRows) / sum(totalRows)` | `*.complete.failedRows / totalRows` |
| **整体结果分布** | 按 `pushResult ∈ {allSuccess, partial, allFail}` 计数 | `*.complete.pushResult` |
| **推送耗时 P50/P95/P99** | `costMs` | `*.complete.costMs` / `*.failed.costMs` |
| **后端 API 耗时 P50/P95** | `api.pushTestCase.ok.costMs` | http 层 |
| **单次推送行数分布** | `totalRows` 直方图 | `*.complete.totalRows` |
| **文件类型分布** | 按 `ext` 分组（yaml / csv / xlsx 等） | `*.complete.ext` |
| **被中断流失分布** | 按 `reason ∈ {multiFile, dirNotQualified, unbound, noData, onlyTemplateExample}` | `explorerPush.aborted.reason` |
| **后端错误码分布** | `returnCode` | `*.failed.returnCode` / `api.pushTestCase.fail.returnCode` |

> 漏斗转化率 = `*.complete / *.start`，对比两个入口可以看出哪条路径用户更顺。

### 2.2 创建测试案例漏斗

| 节点 | 事件 |
|---|---|
| 命令触发 | `command.executed[command=testcaseViewer.createNewTestCase / createNewTestCaseQuick]` |
| 创建成功 | `createNewTestCase.success` |
| 创建异常 | `createNewTestCase.error` / `createNewTestCase.commandError` / `createNewTestCaseQuick.commandError` |
| 创建后跳编辑器失败 | `createNewTestCase.openEditor.failed` |

**指标**：每日新建数、失败率、Quick 入口占比、新建后立即推送转化率（同 sessionId 下后续是否触发 `editorPush.start`）。

### 2.3 在线查询漏斗（TestCaseProvider）

| 节点 | 事件 |
|---|---|
| 入口曝光 | `testCase.webview.shown` |
| Webview 就绪 | `testCase.webview.ready` |
| 查任务树 | `testCase.taskTree.requested` |
| 发起查询 | `testCase.query.requested` |
| 查询成功 / 终止 / 失败 | `testCase.query.success` / `testCase.query.endOfData` / `testCase.query.error` |
| 用户从案例打开"在线查看" | `testCase.viewOnline[bound=true/false]` |

**指标**：曝光 → ready → 查询的转化率、首屏空数据比、平均翻页深度（`currentPage`）、绑定 vs 未绑定打开线上的占比、`api.queryTestCases.done.costMs` 分位线。

### 2.4 表格浏览器漏斗（TableBrowser）

| 节点 | 事件 |
|---|---|
| 命令打开 | `command.executed[command=tableBrowser.open]` |
| 工作区目录加载 | `tableBrowser.workspaceFiles.loaded[nodeTotal]` |
| 选择 → 发送 | `tableBrowser.send.success` / `send.failed` / `send.error` / `send.noSelection` |
| 异常路径 | `tableBrowser.noWorkspace` / `csv.invalidPath` / `csv.empty` / `csv.readFailed` |

**指标**：打开后实际产生选送的比例、CSV 读取失败比、平均工作区文件数（容量曲线）。

### 2.5 任务绑定

| 事件 | 说明 |
|---|---|
| `command.executed[command=testcaseViewer.revealBoundTask]` | 用户主动定位到绑定任务 |
| `bindings.fileChanged` | 绑定文件被修改 |
| `bindings.initFailed` | 启动期初始化失败（健康度） |

**指标**：日活用户中"已完成绑定"的占比（`revealBoundTask` 触发数 / 活跃机器数）、绑定文件变更频次。

---

## 三、编辑器使用质量

| 指标 | 事件 | 解读 |
|---|---|---|
| 自定义编辑器打开次数 | `editor.opened` | 按 `fileFormat` 分维度：yaml/csv/xlsx 哪种用户最多 |
| 文件保存次数 / 频率 | `editor.saved` | 单 sessionId 平均保存次数 |
| 切换为文本编辑器次数 | `editor.switchedToText` | 高占比 → 表格编辑体验欠佳，用户回退 |
| 重新加载次数 | `editor.reloaded` | 同上信号 |
| **打开非合规文件兜底比例** | `editor.opened.unqualified` / `editor.unqualified.openText` | 路径规范覆盖率 |
| 外部修改触发刷新 | `editor.externalChange.fired` vs `.skipped` | git 切换、其他工具改写文件的频次 |
| 自我保存被防抖跳过比例 | `editor.visibleChange.skipped[reason=selfSaveGuard]` | 防抖逻辑生效频次 |
| **编辑器启动 ready 超时率** | `editor.waitReady.timeout` | 性能/兼容性问题 |

### 3.1 YAML 格式校验 & 自动修复

> 插件在用户打开/保存/编辑 `.yaml` / `.yml` 文件时自动进行格式校验（Tab 缩进、行末空格、冒号缺空格、特殊未引号值、YAML 保留字符等），并将问题以内联 Diagnostic 波浪线展示，同时提供 Quick Fix 一键修复。

| 指标 | 计算口径 | 数据来源 |
|---|---|---|
| **每日校验触发次数** | `count(yaml.validation.issues)` 按天聚合 | `yaml.validation.issues` |
| **平均每次校验问题数** | `avg(total)` | `yaml.validation.issues.total` |
| **错误 / 警告占比** | `sum(errorCount) / sum(total)` vs `sum(warningCount) / sum(total)` | `yaml.validation.issues.errorCount / warningCount` |
| **问题数分布（直方图）** | 按 `total` 分桶（1-5 / 6-10 / 11-20 / 20+） | `yaml.validation.issues.total` |
| **文件格式分布** | 按 `ext` 分组 | `yaml.validation.issues.ext` |
| **每日修复次数** | `count(yaml.fix.applied)` 按天聚合 | `yaml.fix.applied` |
| **平均每次修复行数** | `avg(fixCount)` | `yaml.fix.applied.fixCount` |
| **有问题的文件中用户触发修复的渗透率** | `distinct(sessionId from yaml.fix.applied) / distinct(sessionId from yaml.validation.issues)`（同天） | 两个事件关联 |
| **修复后仍然有问题的残留率** | 同 sessionId 同文件，`yaml.fix.applied` 后 5s 内 `yaml.validation.issues.total > 0` 的比例 | 需要同文件时序分析 |

> 校验触发场景：打开文件、保存文件、内容变更（500ms 防抖）、窗口重载（Cmd+R）。修复支持单行修复和全部修复两种模式。

---

## 四、错误与异常（质量看板）

按事件名前缀分组，每个事件都自带 `errorMessage` / `stackHead` 便于聚类。

| 类别 | 关键事件 |
|---|---|
| **推送链路异常** | `editorPush.failed` / `explorerPush.failed` / `editorPush.writeBackFailed` / `explorerPush.writeBackFailed` / `editorPush.allFailSnapshotFailed` / `editorPush.reparseFailed` |
| **后端 API 失败** | `api.pushTestCase.fail` / `api.pushTestCase.exception` / `api.fetchTaskTree.fail` / `api.queryTestCases.done[returnCode != SUC0000]` / `api.batchImport.done[returnCode != SUC0000]` |
| **鉴权失效** | `api.auth.unauthorized[api, httpStatus]`（401/403） |
| **网络层** | `http.request.error[errCode, target]` / `editor.network.error` / `http.sm2.signFailed` |
| **持久化文件健康** | `snapshot.load.failed` / `snapshot.load.invalidFormat` / `snapshot.save.failed` / `snapshot.init.failed` / `snapshot.initFailed` / `fileIdentifier.read.failed` / `fileIdentifier.write.failed` / `fileIdentifier.write.noWorkspace` / `highlight.initFailed` / `pushFailure.initFailed` / `deletedRows.initFailed` |
| **业务规则违常**（必须强报警） | `editorPush.templateExampleLeaked` / `explorerPush.templateExampleLeaked`（样例行漏过过滤） |
| **未捕获异常** | `extension.unhandledRejection` |
| **编辑器异常** | `editor.testcaseId.saveFailed` / `editor.message.error` / `editor.save.error` / `editor.push.error` / `editor.keepEditor.failed` |
| **命令级异常** | `command.openWithEditor.error` / `command.openWithText.error` / `command.tableBrowser.open.error` / `command.viewOnline.error` / `explorerPush.commandError` / `createNewTestCase*.commandError` |

**关键运营指标**：

- **崩溃率** = `extension.unhandledRejection` 用户数 / DAU
- **业务违常率** = `*.templateExampleLeaked` 次数（理想 = 0，>0 即报警）
- **登录态失效率** = `api.auth.unauthorized` 用户数 / 当日活跃用户数
- **后端错误码 Top10**：聚合 `api.*.returnCode`，识别哪个 returnCode 是主要噪音
- **持久化文件损坏率**：`snapshot.load.invalidFormat / snapshot.load.failed` / 总会话数

---

## 五、性能指标

| 指标 | 数据来源 |
|---|---|
| 激活耗时 P50/P95 | `extension.activate.done.activateMs` |
| 推送整体耗时 P50/P95（按行数分桶） | `explorerPush.complete.costMs` / `editorPush.complete.costMs` |
| 推送失败前耗时 | `*.failed.costMs` |
| 推送 API 耗时 | `api.pushTestCase.ok.costMs` / `api.pushTestCase.fail.costMs` |
| 任务树查询耗时 | `api.fetchTaskTree.ok.costMs` / `api.fetchTaskTree.fail.costMs` |
| 测试案例查询耗时 | `api.queryTestCases.done.costMs` |
| 批量导入耗时 | `api.batchImport.done.costMs` |
| 工作区文件加载规模 | `tableBrowser.workspaceFiles.loaded.nodeTotal` |

---

## 六、特性渗透 & 行为画像

| 关注点 | 事件 / 维度 |
|---|---|
| 模板示例行被推送前主动过滤的比例 | `editorPush.skipTemplateExample.skipped` / `explorerPush.skipTemplateExample.skipped` |
| 中止原因画像（用户为啥没推成） | `*.aborted.reason` 分布 |
| 资源管理器 vs 编辑器内推送 二选一占比 | `explorerPush.complete` vs `editorPush.complete` |
| 推送结束后未拿到 panel 的兜底情况 | `explorerPush.noPanelFallback` |
| 删除行同步频次 / 规模 | `syncDeletedRows.complete` / `noPending` / `aborted` |
| 用户偏好的文件类型 | 推送 `complete.ext`、editor `editor.opened.fileFormat` 双维度交叉 |

---

## 七、推荐的 5 张运营仪表盘

按"看一眼能立刻得出结论"的原则，不要堆事件，要堆指标。

1. **总览盘**：DAU/WAU/MAU、版本分布、平台分布、激活耗时 P95、崩溃率
2. **推送质量盘**：行级成功率（趋势）、`pushResult` 堆叠柱、错误码 Top10、推送耗时 P95、aborted 原因 Top
3. **错误盘**：所有 `*.failed / *.error / *.exception` 事件的小时计数 + 报警阈值（含 `templateExampleLeaked` / `api.auth.unauthorized` 强报警）
4. **功能渗透盘**：每个核心命令的日触发用户数（`command.executed`）、核心漏斗的转化率
5. **性能盘**：激活耗时、推送耗时、查询耗时分位线（按版本 / 平台拆分）

---

## 八、附录：完整事件清单

> 当前代码中已落地的全部埋点事件汇总，便于数据侧建模时一次性配置。

### 8.1 生命周期 & 公共

- `extension.activated`
- `extension.activate.done` `{ activateMs }`
- `extension.deactivated` `{ sessionMs }`
- `extension.deactivate`
- `extension.unhandledRejection` `{ errorMessage, stackHead }`

### 8.2 命令执行

- `command.executed` `{ command, [execResult] }`
- `command.aborted` `{ command, reason }`
- 命令异常：`command.openWithEditor.error` / `command.openWithText.error` / `command.tableBrowser.open.error` / `command.viewOnline.error` / `explorerPush.commandError` / `createNewTestCase.commandError` / `createNewTestCaseQuick.commandError`

### 8.3 推送链路

| 资源管理器入口 | 编辑器入口 |
|---|---|
| `explorerPush.start` `{ ext, totalRows }` | `editorPush.start` `{ ext, totalRows }` |
| `explorerPush.aborted` `{ reason, ext }` | `editorPush.aborted` `{ reason, ext }` |
| `explorerPush.skipTemplateExample` `{ ext, skipped }` | `editorPush.skipTemplateExample` `{ ext, skipped }` |
| `explorerPush.complete` `{ ext, totalRows, successRows, failedRows, pushResult, costMs }` | `editorPush.complete` `{ ext, totalRows, successRows, failedRows, pushResult, costMs }` |
| `explorerPush.failed` | `editorPush.failed` |
| `explorerPush.writeBackFailed` | `editorPush.writeBackFailed` |
| `explorerPush.templateExampleLeaked` `{ ext, leakedSuccess, leakedFailure }` | `editorPush.templateExampleLeaked` `{ ext, leakedSuccess, leakedFailure }` |
| `explorerPush.noPanelFallback` `{ succ, fail, ext }` | `editorPush.allFailSnapshotFailed` |
| | `editorPush.reparseFailed` |

### 8.4 后端 API

- `api.fetchTaskTree.start`
- `api.fetchTaskTree.ok` `{ returnCode, costMs }`
- `api.fetchTaskTree.fail` `{ returnCode, costMs }`
- `api.queryTestCases.done` `{ returnCode, currentPage, costMs }`
- `api.batchImport.done` `{ returnCode, totalRows, costMs }`
- `api.pushTestCase.ok` `{ httpStatus, totalRows, bytes, costMs }`
- `api.pushTestCase.fail` `{ httpStatus, returnCode, totalRows, costMs }`
- `api.pushTestCase.exception` `{ totalRows, errorMessage, stackHead }`
- `api.auth.unauthorized` `{ api, httpStatus }`
- `http.request.error` `{ errCode, target }`
- `http.sm2.signFailed` `{ errorMessage, stackHead }`

### 8.5 创建测试案例

- `createNewTestCase.success`
- `createNewTestCase.error`
- `createNewTestCase.openEditor.failed`

### 8.6 自定义编辑器

- `editor.opened` `{ fileFormat }`
- `editor.opened.unqualified` `{ targetFile }`
- `editor.unqualified.openText` `{ targetFile }`
- `editor.saved`
- `editor.reloaded`
- `editor.switchedToText`
- `editor.waitReady.timeout` `{ targetFile }`
- `editor.keepEditor.failed`
- `editor.testcaseId.saveFailed`
- `editor.visibleChange.skipped` `{ reason, fileFormat }`
- `editor.visibleChange.failed`
- `editor.externalChange.skipped` `{ origin, fileFormat }`
- `editor.externalChange.fired` `{ origin, fileFormat }`
- `editor.message.error`
- `editor.save.error`
- `editor.push.error`
- `editor.network.error`

### 8.7 在线查询（TestCaseProvider）

- `testCase.viewOnline` `{ bound }`
- `testCase.webview.shown`
- `testCase.webview.ready`
- `testCase.taskTree.requested`
- `testCase.query.requested` `{ currentPage }`
- `testCase.query.success`
- `testCase.query.endOfData`
- `testCase.query.error` `{ queryError }`
- `testCase.message.error`

### 8.8 表格浏览器（TableBrowser）

- `tableBrowser.noWorkspace`
- `tableBrowser.workspaceFiles.loaded` `{ nodeTotal }`
- `tableBrowser.csv.invalidPath`
- `tableBrowser.csv.empty`
- `tableBrowser.csv.readFailed`
- `tableBrowser.send.noSelection`
- `tableBrowser.send.success` `{ totalRows }`
- `tableBrowser.send.failed` `{ returnCode }`
- `tableBrowser.send.error`

### 8.9 任务绑定

- `bindings.fileChanged`
- `bindings.initFailed`

### 8.10 持久化文件健康

- `snapshot.load.failed`
- `snapshot.load.invalidFormat`
- `snapshot.save.failed`
- `snapshot.init.failed`
- `snapshot.initFailed`
- `fileIdentifier.read.failed`
- `fileIdentifier.write.failed`
- `fileIdentifier.write.noWorkspace`
- `highlight.initFailed`
- `pushFailure.initFailed`
- `deletedRows.initFailed`

### 8.11 删除行同步

- `syncDeletedRows.aborted` `{ reason }`
- `syncDeletedRows.noPending`
- `syncDeletedRows.complete`
- `syncDeletedRows.error`

### 8.12 YAML 格式校验 & 修复

- `yaml.validation.issues` `{ total, errorCount, warningCount, ext }`
- `yaml.fix.applied` `{ fixCount }`

---

**版本**：v1.1  
**生成日期**：2026-07-09  
**对应代码**：基于 `src/utils/telemetry.ts` / `src/services/http.ts` 及全部 providers 中的 `sendTelemetryEvent / sendTelemetryErrorEvent` 调用统计
